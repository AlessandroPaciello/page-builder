{
  "name": "@penpot-ds/scripts",
  "version": "0.0.0",
  "private": true,
  "description": "Pipeline MCP Penpot→codice: Stage 1 genera i token di @penpot-ds/tokens, Stage 2 estrae per-componente fixture e ricette validate (schema + vocabolario token).",
  "type": "module",
  "scripts": {
    "generate:theme": "node --import tsx src/generate-theme.ts",
    "extract:component": "node --import tsx src/extract-component.ts extract",
    "validate:recipe": "node --import tsx src/extract-component.ts validate",
    "test": "vitest run",
    "check-types": "tsc --noEmit",
    "lint": "node ./scripts/check-boundaries.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "catalog:",
    "@penpot-ds/tokens": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@app/config": "workspace:*",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
