{
  "name": "@penpot-ds/scripts",
  "version": "0.0.0",
  "private": true,
  "description": "Pipeline MCP che legge il catalogo token da Penpot e genera i file di @penpot-ds/tokens. Stage 1 (token) solo — estrazione componenti/varianti è Story 2.2/2.3.",
  "type": "module",
  "scripts": {
    "generate:theme": "node --import tsx src/generate-theme.ts",
    "extract:component": "node --import tsx src/extract-component.ts extract",
    "validate:recipe": "node --import tsx src/extract-component.ts validate",
    "test": "vitest run",
    "check-types": "tsc --noEmit",
    "lint": "node ./scripts/check-boundaries.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "catalog:",
    "@penpot-ds/tokens": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@app/config": "workspace:*",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
