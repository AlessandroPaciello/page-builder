# Deferred Work

Lavoro identificato durante le review e rimandato consapevolmente. Ogni voce indica da dove arriva e quando va ripresa.

## Deferred from: code review of 1-1-scaffolding-greenfield-del-workspace (2026-07-26)

- **Nessuna copertura di test né CI** — non esiste alcun file di test, nessun task `test` in `turbo.json`, nessun workflow in `.github/` (solo `CODEOWNERS` e `PULL_REQUEST_TEMPLATE.md`). Fuori scope per contratto: le Testing Requirements della Story 1.1 escludono esplicitamente test applicativi. Da riprendere in **Story 1.2** (`packages/domain`), che porta il primo caso d'uso testabile: lì va introdotto anche il task `test` in turbo.json e il workflow CI.
- **Nessuna protezione CSRF sulle rotte RPC** — **RISOLTO in Story 1.5 (2026-09-05)**, prima della prima mutation RPC (`notes.create`): guard Origin/CSRF in `apps/web/src/app/api/rpc/[[...rest]]/route.ts` via `verifyTrustedOrigin` (`packages/api/src/origin-guard.ts`, puro e testato). Per i metodi state-changing (POST/PUT/PATCH/DELETE) verifica l'header `Origin` (fallback `Referer`, confronto per origin dopo `new URL().origin`) contro l'allow-list da `CORS_ORIGIN` letta da `process.env` direttamente; senza Origin valido → 403. Fail-closed anche sulla config: `CORS_ORIGIN` mancante → lista vuota → mutation negate (i GET restano senza guard: le procedure di lettura non mutano). Restano da notare: il client oRPC same-origin (`orpc.ts`, `credentials: "include"`) trasmette sempre `Origin` **dal browser** (nessun impatto sulle mutazioni lato browser); ma lo stesso link è usato server-side (`orpc.ts:41-50` inoltra solo cookie/authorization, senza `Origin` — undici non lo aggiunge): **la prima mutation invocata da RSC/server action sarà negata 403 fail-closed** — rivalidare quando il percorso SSR si attiva (vedi voce "Percorso SSR di oRPC è codice morto", Epic 4/6); i client RPC programmatici senza browser sono esclusi dalle mutation (accettabile: la via autenticata è il browser); i cookie Better Auth mantengono il default SameSite `lax` — **CHIUSO in Story 1.6 (2026-09-06)**: SameSite=Lax verificato EMPIRICAMENTE su compose release reale (`Set-Cookie` di sign-up/sign-in nel container), stessa topologia origin del compose rende `lax` corretto, nessuna configurazione esplicita necessaria.
- **Hardening dell'autenticazione assente** — **RISOLTO in Story 1.4 (2026-09-05)**, con due esiti parziali rimasti aperti: `minPasswordLength: 8` e `rateLimit` esplicito (window 60s / max 10, con le special rules di Better Auth su `/sign-up/*` e `/sign-in/*`) sono ora configurati server-side in `packages/auth/src/index.ts` e coperti da test di integrazione, incluso un test che verifica davvero il 429 sulle special rules (`packages/auth/tests/auth.integration.test.ts`). Regola dedicata `/get-session: false` in `rateLimit.customRules` (rotta di lettura chiamata a ogni navigazione/tab). `advanced.ipAddress.trustedProxies` — **CHIUSO in Story 1.6 (2026-09-06)**: ora alimenta `env.TRUSTED_PROXIES` (`packages/env/src/server.ts`, opzionale, default `[]`); il compose di release dichiara la subnet `172.28.0.0/16` e la passa come `TRUSTED_PROXIES`. Restano aperti: **verifica email disattivata per scelta documentata** (vedi voce dedicata qui sotto, aggiornata in 1.6) e **assegnazione ruoli senza endpoint** (voce dedicata qui sotto).
- **Verifica email (`requireEmailVerification`) disattivata per scelta documentata** — il campo `emailVerified` esiste sul modello `User` ma non è mai usato: Better Auth non richiede la verifica per autenticare. **CHIUSO in Story 1.6 (2026-09-06) per quanto concerne l'envelope**: `requireEmailVerification` è ora flag env `REQUIRE_EMAIL_VERIFICATION` (default `false`, mai hard-coded — la decisione di attivarla al rilascio resta di Alessandro) con callback `sendVerificationEmail` stub su stdout (`packages/auth/src/index.ts`), verificata su compose reale (flag ON → link su stdout + sign-in 403 `EMAIL_NOT_VERIFIED`; flag OFF → sign-in OK). Attivare un provider email reale resta **Deferred** alla prima topologia che lo richiede.
- **Assegnazione ruoli senza endpoint** — la matrice RBAC definisce cosa può fare ciascun ruolo, non chi assegna i ruoli. Il sign-up produce solo CLIENTE (`input: false` su `additionalFields.role` + default colonna, testato): nessun utente nasce ADMIN/EDITOR. La promozione a EDITOR/ADMIN oggi è possibile solo via SQL/seed manuale in dev; gli endpoint di gestione utenti (che il plugin `admin` di Better Auth offrirebbe) sono volutamente fuori scope (Dev Notes Story 1.4). Serve un meccanismo esplicito (plugin admin, seeded del primo ADMIN, o endpoint amministrativo) alla **prima story che ne ha bisogno** — probabile **Story 1.5** (Principal nel context oRPC) o Epic 2+.
- **Accessibilità dei form di autenticazione** — gli `<Input>` in `apps/web/src/components/sign-in-form.tsx` e `sign-up-form.tsx` non hanno `aria-invalid` né `aria-describedby` verso il messaggio d'errore, e gli errori non sono in una live region. Contrasta con `_bmad-output/specs/spec-page-builder/a11y-baseline.md`. Da riprendere in **Epic 2** (libreria primitive accessibili), che riscrive questi componenti.
- **Stati di caricamento/errore non gestiti nelle pagine demo** — `apps/web/src/app/dashboard/dashboard.tsx:7-11` non distingue `isLoading` da `isError` (mostra "API: " vuoto); `apps/web/src/app/page.tsx:31-35` colora il pallino di rosso già durante il caricamento. È codice demo del template, sostituito in **Epic 4** (shell editor).
- **Percorso SSR di oRPC è codice morto** — `apps/web/src/utils/orpc.ts:32-77`: `getQueryClient()` per-richiesta, il forwarding di `cookie`/`authorization` e la risoluzione URL server-side esistono solo per lo scenario prefetch/SSR, ma nel diff non c'è un solo `HydrationBoundary`, `prefetchQuery` o chiamata `client.*` da un Server Component. Manca anche un `MutationCache.onError` e un `AbortSignal.timeout` sul fetch loopback. I due commit di fix (`bd377bc`, `e969052`) hanno hardenizzato un percorso che nessuno esegue e nessun test copre. Si attiva in **Epic 4/6** (autosave, render pubblico SSG/ISR): rivalidare allora.
- **`output: "standalone"` assente in `apps/web/next.config.ts`** — Spine#Envelope operativo prevede deploy container Docker self-host con Next in modalità standalone. Non richiesto dagli AC della Story 1.1. **CHIUSO in Story 1.6 (2026-09-06)**: `output: "standalone"` attivo, Dockerfile multi-stage (deps/builder/runner/migrator) alla root, compose di release `docker-compose.release.yml`, E2E verificato localmente.
- **Config TypeScript cargo-cult nei package** — `packages/{api,auth,db}/tsconfig.json` dichiarano `composite: true`, `outDir: "dist"`, `declaration`/`declarationMap`/`sourceMap`, ma gli script sono `tsc --noEmit` e nessun tsconfig usa `references`: nulla viene mai emesso. Suggerisce un build a project references che non esiste. Da ripulire o da rendere reale quando i package avranno un vero step di build.
- **`check-types` e `build` non dipendono da `db:generate`** — **RISOLTO in Story 1.3 (2026-09-05)**: `@app/db#check-types` esegue ora `prisma generate && tsc --noEmit` (`packages/db/package.json`), quindi ogni `turbo run check-types`/`build` (che dipendono da `^check-types`) rigenera il client dallo schema corrente prima che qualsiasi consumer faccia type-check. Il client generato resta comunque committato per build riproducibili.
- **Docker compose di sviluppo poco robusto** — `packages/db/docker-compose.yml`: porta host `5432` hardcoded (`db:start` fallisce se esiste già un Postgres locale) e `image: postgres:18` non pinnata a una patch. Solo ambiente di sviluppo locale, nessun impatto su release. La pinnatura è **CHIUSA in Story 1.6 (2026-09-06)** (`postgres:18.1`); la porta hardcoded resta (limite noto, dev-only).

## Deferred from: code review of 1-1-scaffolding-greenfield-del-workspace (2026-09-05)

- **Robustezza del menu a tendina scritto a mano** — `packages/ui/src/editor/dropdown-menu.tsx`: due menu aperti insieme condividono lo stesso Escape/focus globale (nessuna ownership tra istanze), nessun portal/collision detection (un antenato con `overflow:hidden`/`auto` lo taglia, nessun flip vicino al bordo viewport), `ArrowUp` con `activeElement` fuori dagli item salta al penultimo invece che all'ultimo, `{...props}` spreadato dopo `ref`/`onKeyDown` su `DropdownMenuContent` permette a un consumer di sovrascriverli silenziosamente, ARIA incompleta (`aria-controls`/`id`/`aria-labelledby` mancanti), `Tab` chiude il menu senza `preventDefault()`. Nessuno dei due call site attuali (`mode-toggle`, `user-menu`) raggiunge questi percorsi. È il tradeoff esplicito della decisione "potatura + menu a mano" presa il 2026-09-05 per rimuovere `@base-ui/react`. Da rivedere quando `domains/` inizia a generare il primitivo reale (**Story 2.10**, ex 2.4, poi 2.7, poi 2.9 — rinumerata dai correct-course del 2026-09-12 e 2026-09-13), o prima se questo menu deve reggere più a lungo del previsto.
- **Alias shadcn puntati al barrel invece che a una directory** — `packages/ui/components.json`, `apps/web/components.json`: `"ui"`/`"components"` risolvono a `@penpot-ds/ui/editor` (il barrel `index.ts`). Il CLI shadcn fa la propria risoluzione di path e scriverebbe comunque nel posto giusto, ma un componente aggiunto così resta invisibile ai consumer finché qualcuno non aggiorna a mano `editor/index.ts`. Non esercitato da questa story — nessuno lancia il CLI. Da sistemare in **Story 2.6** (Task 0), quando l'emitter inizia davvero a usare il CLI shadcn (ex Story 2.3; correct-course 2026-09-12).
- **Normalizzazione minima degli URL d'ambiente** — `packages/env/src/server.ts`, `packages/env/src/web.ts`: `BETTER_AUTH_URL`/`CORS_ORIGIN`/`NEXT_PUBLIC_SERVER_URL` vengono normalizzati solo sullo slash finale, non su case, query string o protocollo. Accettabile perché sono valori di configurazione impostati da chi fa il deploy, non input di un utente esterno. Da rivedere solo se questa premessa smette di valere.
- **`prisma.config.ts` non aggiunge un messaggio d'errore proprio se `.env` manca** — `packages/db/prisma.config.ts`: l'helper `env()` di Prisma lancia già un errore leggibile in quel caso. Da rivedere solo se si rivelasse insufficiente in pratica.

## Deferred from: code review of 1-2-skeleton-del-core-di-dominio-esagonale (2026-09-05)

- **save-then-audit senza atomicità** — `packages/domain/src/use-cases/create-note.ts:29-36`: `repository.save` poi `auditWriter.write`; se il write fallisce resta una mutazione persistita non tracciata e il retry del chiamante duplica la nota. **Ri-mirata in Story 1.3 (2026-09-05)**: qui è arrivato solo lo schema DB (nessun repository concreto, per lezione della Story 1.2). La decisione richiesta ora ha il contesto completo: la strategia indicata è **transazione DB del repository concreto** (`prisma.$transaction` che persiste la mutazione e scrive l'audit log nella stessa transazione, tabella `audit_log` ora esistente con forma AD-8). Da risolvere in **Story 4.2** (salvataggio bozza con snapshot immutabile, primo repository concreto con mutazioni reali) — o in **Story 5.4** se l'audit trail richiede ulteriore infrastruttura.
- **Create senza check di id duplicato** — `packages/domain/src/use-cases/create-note.ts:29` + `ports/repository.ts`: un `input.id` già presente sovrascrive in silenzio (upsert) con audit `note.created`; nessun precondition `findById` né path di conflitto. **Ri-mirata in Story 1.3 (2026-09-05)**: lo schema ora fornisce i vincoli DB su cui basare la semantica (`page.slug` univoco, `page_version` UNIQUE(pageId, versionNumber), `page_assignment` UNIQUE(pageId, userId) — violazioni emergono come Prisma `P2002`). La decisione di semantica (upsert esplicito vs. errore di conflitto mappato) resta da prendere insieme al primo repository concreto: **Story 4.2**.

## Deferred from: code review of 1-3-modello-dati-e-invariante-di-pubblicazione-a-livello-db (2026-09-05)

- **Check di drift schema↔migrations in CI** — nessun passo blocca uno `schema.prisma` che evolve senza migration committata (es. `prisma migrate diff --from-migrations ./prisma/migrations --to-schema-datamodel ./prisma/schema/schema.prisma --exitcode`); il drift viene scoperto solo al `migrate deploy` di release. **CHIUSO in Story 1.6 (2026-09-06)**: step CI "Drift check (schema ↔ migrations)" in `.github/workflows/ci.yml` con `prisma migrate diff --from-migrations ./prisma/migrations --to-schema ./prisma/schema --exit-code` (sintassi Prisma 7: `--exit-code`, `--to-schema`; serve `SHADOW_DATABASE_URL` — `datasource.shadowDatabaseUrl` opzionale in `prisma.config.ts`, DB dedicato `ci_shadow` creato nel job). Percorso rosso verificato empiricamente (migration rimossa → exit 2 → colonna `role` rilevata), verde (exit 0).
- **Nessun CHECK su `versionNumber`** — `page_version.versionNumber` (INTEGER) accetta 0/negativi; un `CHECK (versionNumber >= 1)` nella migration costerebbe una riga. Non richiesto dalla forma minimale di spec (AD-6 affida l'allocazione al core); da valutare alla prima mutation che tocca la tabella.
- **Verifica di lockstep del client generato committato** — il client `packages/db/prisma/generated/` resta committato per build riproducibili, ma `check-types` lo rigenera in place: un client committato stale non viene mai smascherato (la rigenerazione lo ripara silenziosamente). Un passo CI `prisma generate && git diff --exit-code packages/db/prisma/generated` renderebbe la convenzione verificata invece che affidata alla disciplina.

## Deferred from: code review of 1-4-autenticazione-con-ruoli-admin-editor-cliente (2026-09-05)

- **Storage del rate limit in-memory** — `packages/auth/src/index.ts:34-38` non configura `rateLimit.storage`/`secondaryStorage`: i contatori vivono in una `Map` per-processo (`better-auth/dist/api/rate-limiter/index.mjs`), azzerati a ogni restart e moltiplicati per N con più istanze. **CHIUSO in Story 1.6 (2026-09-06) con decisione documentata**: per il compose release single-instance è accettabile; la limitazione è documentata nel README (§ Deploy → Limitazioni). Storage condiviso (DB/Redis) alla prima topology multi-istanza, non prima.
- **Guard `/dashboard` non testato a livello rotta** — il ramo `safeParseRole` → redirect (`apps/web/src/app/dashboard/page.tsx:19-22`) è l'unico comportamento nuovo della pagina senza coverage: l'AC#2 è coperto a livello libreria (spec dichiara accettabile il client test) ma un test di rotta richiede infra E2E. Rivalidare in **Story 1.5** (Principal nel context) o Epic 2.
- **Ruolo fuori tassonomia con sessione valida → loop login↔redirect** — sessione valida + `user.role` corrotto rimanda a `/login`, il login ricrea la stessa sessione: loop senza feedback. Oggi irraggiungibile via flow normale (colonna con default + `input: false`), rischio solo da scritture esterne; la risposta giusta sarebbe una 403/pagina di errore. Da **Story 1.5+** con Principal nel context oRPC.

## Deferred from: code review of 1-5-context-orpc-con-principal-e-policy-deny-by-default (2026-09-06)

- **Dettagli della policy nel canale client** — `toORPCError` (`packages/api/src/errors.ts`) inoltra `error.message` dentro `ORPCError("FORBIDDEN")`, e `ForbiddenError` (`packages/domain/src/authz.ts`) compone messaggi con `allowedRoles`: dettagli della policy interna finiscono nel canale client per costruzione. Innocuo per il fixture (la matrice RBAC è nota al client), ma è il meccanismo che le procedure reali di **Epic 4/5** ereditano: decidere lì la forma dei messaggi esposti (e la regola 404-per-non-rivelazione si inserisce nella stessa funzione).
- **Nessun test automatico degli script `check-boundaries.mjs`** — né `packages/api/scripts/check-boundaries.mjs` né il gemello di `packages/domain` hanno test: ~240+ righe di parser/regex verificate solo da canary manuale. Una regressione futura spegne il confine in silenzio restando verde — l'unico failure mode che un gate fail-closed non copre da solo. Da coprire con canary automatizzati (rosso/verde) in una story di consolidamento o alla prima modifica degli script.
- **Nessun test del wiring rotta RPC** — guard Origin + `createContext` + handler (`apps/web/src/app/api/rpc/[[...rest]]/route.ts`) sono testati solo separatamente; il punto in cui si incontrano (ordine 404→guard→context, le decisioni d'ordine che la story ha realmente mosso) è senza coverage: `apps/web` non ha harness di test ed E2E assente repo-wide. Stesso problema familiare della voce 1-4 "guard /dashboard non testato a livello rotta": rivalidare quando arriva infra E2E (Epic 2+).

## Deferred from: dev-story of 2-1-pipeline-token-penpot-codice (2026-09-06)

- **Scala/options/map TS limitate a `spacing`/`borderRadius`** — `theme-generator.ts` genera i field select Puck solo per questi due tipi (minimo richiesto dall'AC); `borderWidth`, `opacity`, `fontWeights`, `fontSizes` hanno già CSS custom properties ma non un equivalente `*Options`/`*Map` in `tokens.generated.ts`. `collectByType()` è già generico per tipo: estendere è un one-liner quando un consumer (blocco Puck) ne avrà bisogno — probabile Story 2.2+/Epic 3.
- **`ignoreDeprecations: "6.0"` in `packages/tokens/tsconfig.json`** — necessario perché `tsup@8.5.1` (dts build via rollup-plugin-dts) inietta sempre `compilerOptions.baseUrl` per risolvere gli import non relativi, e TypeScript `~6.0` tratta `baseUrl` come deprecato-quasi-errore (TS5101). Nessun `baseUrl` è impostato altrove nel repo: è un difetto di compatibilità tsup↔TS6, non una scelta del progetto. Da rimuovere quando una versione di `tsup` smette di iniettarlo, o se TS lo rimuove del tutto in 7.0 (a quel punto sarà un errore hard, non silenziabile). `packages/tokens` è il primo (unico) consumer di `tsup` nel repo: chiunque aggiunga un secondo package con build `tsup --dts` incontrerà lo stesso problema.
- **Parsing della risposta MCP `execute_code` in `penpot-reader.ts` dedotto empiricamente** — l'envelope `{ content: [{ type: "text", text: '{"result":...,"log":...}' }] }` è stato osservato dal comportamento reale del tool durante l'estrazione della fixture in questa story (sessione MCP live, container `penpot-penpot-mcp-1`), non da uno schema pubblicato. Se una futura versione del server MCP Penpot cambia questo envelope, `extractCatalog()` fallisce esplicitamente (nessun parsing silenzioso) ma va aggiornato. Verificato funzionante end-to-end in questa story (`generate:theme -- --live` contro il server reale).

## Deferred from: code review of 2-1-pipeline-token-penpot-codice (2026-09-06)

- **Struttura mode/theme (es. dark mode) non modellata** — `theme-generator.ts` (`buildIndex`) rifiuta come duplicati nomi token identici in set diversi, e il `TokenCatalog` non ha alcuna dimensione mode/theme: la convenzione light/dark standard (stessi nomi in set di mode diversi) non è ingeribile. Funziona per l'attuale catalogo flat; il primo dark mode richiede di ridisegnare il contratto di serializzazione. **Aggiornato in Story 2.4 (2026-09-12):** la nuova library ha i due set `palette` + `semantic`, nessun theme: la voce resta aperta, la nuova library non cambia il contratto di serializzazione.
- **`penpot-reader.ts` privo di copertura test** — lo spec Task 4 richiede test solo sul mapper puro; il parsing envelope/gestione errori/timeout del client MCP resta senza coverage. Da coprire con transport mock quando il reader crescerà in Story 2.2/2.3 (lettura componenti/varianti). **CHIUSO in Story 2.2 (2026-09-06) per la parte component-reader**: `component-reader.test.ts` copre envelope valido/malformato/errore tool, componente non trovato, shape non-variant, celle non mappabili, hex fuori token (stop-signal) e timeout via transport mockato (seam `callTool`), zero rete. Resta scoperto solo il vecchio `penpot-reader.ts` del catalogo token (stesso pattern, già verificato end-to-end live in Story 2.1); da estendere allo stesso seam se si tocca di nuovo.
- **Correzione dei 5 token tracking nell'UI di Penpot — SUPERATA (Story 2.4, 2026-09-12)**: la library `mis` non è più la sorgente dello Stadio 1 (sostituita dalla nuova library con token semantici, tutti con unità esplicita); la correzione nell'UI di Penpot non serve più. Voce originale: durante la code review la Decision 3 (rifiuto fail-loud dei bare number per `letterSpacing`) ha reso necessaria l'unità esplicita sui 5 token `mis.typography` con bare number (`letterSpacing.mis.none` `0`→`0px`, `.tight` `0.5`→`0.5px`, `.wide` `2`→`2px`, `.wider` `3`→`3px`, `typography.spacing.label` `1.5`→`1.5px`). La scrittura via MCP è impossibile (API plugin read-only, verificata empiricamente: `execute_code` espone solo snapshot piatti), quindi la fixture committata è stata corretta a `px` per intento esplicito di Alessandro; **i medesimi valori vanno corretti nell'UI di Penpot** (plugin Tokens) per riallineare la fonte di verità. Finché non avviene, `generate:theme --live` fallirà loud sul tracking bare (by design: nessuna scrittura corrotta, la fixture viene toccata solo post-successo). *(Nota Story 2.2: la API plugin è read-only per i token (`applyToken` non persiste headless), ma le mutazioni dirette di proprietà shape — es. `shape.fills = [...]` — funzionano a pagina attiva: il fill `#ffb4ab` della cella Badge Red/soft è stato corretto a `#ffdad6` via `execute_code` in questa story, su autorizzazione di Alessandro. Il binding esplicito a token in Penpot resta comunque non ottenibile via MCP.)*

## Deferred from: dev-story of 2-4-bootstrap-della-library-penpot-sui-contratti (2026-09-12)

- **Testo warning su background 4.44:1, sotto soglia** — nel seed della nuova library `color.warning` (`#8F6C00`) su `color.background` (`#FAF4E8`) ha contrasto 4.44:1, sotto la soglia 4.5:1 per il testo. La coppia è ammessa SOLO come fill+foreground (il `warning-foreground` bianco su `warning` passa a 4.9:1) e `verifyLibrary` la controlla in quella forma; il testo warning su background resta fuori dalle coppie finché il designer non sceglie un tono più scuro di `amber.9`. Da chiudere con una scelta di tono in Penpot (poi il seed si allinea con la modalità additiva).
- **`applyToken` non supporta i token `fontFamilies`** (Penpot 2.17.2, verificato in Story 2.4): qualunque forma (`["fontFamilies"]`, default, `["all"]`, `applyToShapes`, `["typography"]`) fallisce con "Value not valid: Field 1 is invalid: should be a set of strings". Il font family resta una scelta del designer in Penpot: i design JSON non lo legano e il reader lo esclude dalle proprietà soggette a binding. Da riverificare a un aggiornamento di Penpot se l'emitter (2.6) ne avrà bisogno.

## Register: note di pipeline (Story 2.2)

- **Input/Accordion restano da estrarre in Story 2.3** — verificati presenti in Penpot (`Input / Legacy`, `Accordion / Item`, `Accordion / Default`, pagina Docs) ma esplicitamente fuori scope di Story 2.2 ("estrarre un **singolo** componente", AC #1): la progressione di complessità crescente Badge→Input→Accordion è la validazione della pipeline prima di generalizzare (epics.md#Story 2.3). **Superata (correct-course 2026-09-12):** Input e AccordionItem si estraggono dalla **nuova** library in Story 2.5; `Input / Legacy` e `Accordion / *` del file attuale non sono più input.
- **Pattern "hex-match invece di `shape.tokens` esplicito"** — i componenti Penpot NON hanno binding token nativi (`shape.tokens` quasi sempre vuoto; verificato su Badge in Story 2.2, dove `applyToken` headless non persiste). `component-reader.ts` deriva quindi il binding per **corrispondenza di valore esatto** (hex case-insensitive, radius numerico con riferimenti `{...}` risolti) contro il catalogo Stadio 1 committato, con preferenza deterministica per i token semantici (`color.mis.*`/`color.feedback.*`/`radius.mis.*`) sugli alias di scala (`accent.9`, `radius.full`). Un valore senza corrispondenza esatta è uno **stop-signal esplicito** (errore che nomina shape e valore). Ogni estrazione futura dovrà rifare lo stesso lookup finché il team design non applica `shape.applyToken` nei file Penpot. **RISOLTO in Story 2.4 (2026-09-12, spike Task 1):** `applyToken` via MCP **persiste** dopo il reload del file (verificato su "Page Builder DS": set + token colore ricreati, board con fill legata via `shape.applyToken(token, ["fill"])`, `shape.tokens = { fill: "spike.color" }` letto identico dopo chiusura/riapertura file e riconnessione plugin). La nota "headless non persiste" di Story 2.2 era imprecisa. **`shape.tokens` è la via primaria per la Story 2.5**; l'hex-match resta un fallback per i componenti della vecchia library `mis` finché esistono.
- **La lettura varianti richiede la mappa cella↔board** — le board figlie di un `VariantContainer` NON espongono `variantProps` direttamente: i props stanno su `variants.variantComponents()`, da mappare alle board via `shapeStructure(board, 1).componentInstance.componentId`. Story 2.3 (Input/Accordion) eredita il pattern da `component-reader.ts`.

## Deferred from: code review of 2-2-estrazione-componenti-e-schema-delle-ricette (2026-09-06)

- **Nessuna verifica incrociata ricetta↔fixture su variant keys/`defaultVariants`** — `packages/scripts/src/validate-recipe.ts` verifica solo schema Zod + vocabolario token (esattamente ciò che chiede AC #2 della Story 2.2), non che le chiavi `cva`/`defaultVariants` di una ricetta corrispondano davvero a `variantAxes`/celle della fixture. Una ricetta scritta a mano potrebbe derivare (mappatura sbagliata, combinazione inventata o mancante) restando comunque valida. Il workaround `colorStyle` a 12 chiavi composte in `badge.recipe.json` (compromesso documentato per l'assenza di `compoundVariants` nello schema `RecipeSchema.cva`) non ha un invariante testato che ne verifichi la copertura 4×3 completa. Da riprendere quando Story 2.3 costruisce il gate CI `validate:recipe` e/o il renderer, che avranno bisogno della stessa correlazione. **Assorbita (correct-course 2026-09-12)** dal gate *Conformità al contratto* (Story 2.6) e dalla validazione contro il contratto (Story 2.5); `colorStyle` sparisce con la sostituzione della ricetta Badge.
- **Nuove importazioni cross-package ampliano la superficie del boundary-check già fragile** — `packages/scripts/src/token-vocabulary.ts`/`mcp-client.ts`/`recipe-schema.ts` importano da `theme-generator.ts`; il gate `check-boundaries.mjs` (`packages/scripts/scripts/`) è già segnalato aggirabile e senza test propri dalla retro di Epic 1 (azione aperta lì, owner Charlie/Dev). Non causato da questo diff, ma la superficie di codice che il gate dovrebbe proteggere continua a crescere prima che il gate stesso sia testato.

## Deferred from: code review of 1-6-envelope-docker-e-migration-in-release (2026-09-06)

- **`migrate` e `app` buildano lo stesso `Dockerfile` con due blocchi `build:` separati in `docker-compose.release.yml`** — nessun meccanismo lega le due immagini allo stesso stato sorgente: un operatore che ricostruisce ad-hoc (es. solo `docker compose build app` dopo un fix, senza rifare anche `migrate`) può far girare un `page-builder-migrate:latest` stale contro un `page-builder-app:latest` nuovo, o viceversa. Non bloccante per il flusso CI attuale (checkout unico, stesso commit per entrambi i target); da rivalutare se la release smette di essere un `docker compose build` singolo (es. pipeline con push/pull di immagini taggate per digest).
- **`next-env.d.ts` flip-flop dev↔build** (emerso post-merge, 2026-09-06) — `apps/web/next-env.d.ts` è auto-generato da Next (con `typedRoutes: true`) e ogni `next dev` lo riscrive da `./.next/types/routes.d.ts` a `./.next/dev/types/routes.d.ts`: dopo un dev locale il working tree appare sporco rispetto a un file committato. Fastidioso, non bloccante; il file va tenuto committato (convenzione Next). Valutare alla prossima upgrade Next (potrebbe essere normalizzato a monte) o se il rumore diventa fastidioso: opzioni sono normalizzare via script pre-commit o escludere la riga dall'index con un driver custom — nessuna delle due senza costo.

## Deferred from: correct-course 2026-09-12 (forge "Library Penpot shadcn + esagono Puck")

- **Emitter per una seconda libreria (es. MUI) non provato** — la fattibilità è dedotta dall'API tema MUI (`theme.components.*.variants`/`styleOverrides`), non eseguita. Registrato nel Deferred dello spine (AD-11); da riaprire alla prima richiesta concreta di una libreria non shadcn.
- **Adeguamento di `RecipeSchema` su codice in main** — il passaggio da classi Tailwind a celle `proprietà → token` per parti tocca `packages/scripts` con 88 test esistenti. Va fatto in **Story 2.5** senza far scendere la copertura su `component-reader`, `mcp-client` e CLI; `badge.fixture.json`/`badge.recipe.json` si sostituiscono per estrazione dalla nuova library, non a mano.

## Deferred from: dev-story of 2-3-package-dei-contratti (2026-09-12)

- **Gli altri cinque `check-boundaries.mjs` restano senza test propri** — `packages/contracts/scripts/check-boundaries.mjs` nasce con il modello da seguire: allowlist, funzione pura `checkBoundaries({ packageRoot })` esportata con tipi `.d.mts`, CLI dietro la guardia di invocazione diretta, prova rosso/verde su package finti in tmpdir più il package reale verde (`tests/check-boundaries.test.ts`). `domain`, `api`, `ui` e `tokens` restano a denylist e senza test. *(Aggiornato il 2026-09-14, riordino di `packages/scripts`: `scripts` ha il canary rosso/verde dalla Story 2.4 e ora il gate risolve gli specifier relativi rispetto al file, rosso se escono dalla radice del package — il buco dei relativi è chiuso per `scripts`.)* L'action item della retro Epic 1 resta **open**; estenderlo agli altri package era fuori scope della Story 2.3.
- **Falso positivo fail-closed dello scanner dei contratti** — gli specifier sono cercati con i literal intatti, così una stringa che contiene `from "react"` o `require("x")` è segnalata. È voluto (in `src/` dei contratti non esiste un uso legittimo) ed è coperto da un test "rosso atteso"; da rivalutare solo se un messaggio di errore dei contratti dovesse citare un import.

## Deferred from: code review of 2-3-package-dei-contratti (2026-09-12)

- **Fingerprint fragile per costruzione** — `z.toJSONSchema` lancia su schemi Zod legittimi e probabili in futuro (transform, refinement custom, tipi non-JSON): il test del fingerprint crasherebbe invece di produrre un diff informativo. Inoltre l'output JSON Schema non è garantito stabile tra minor di zod: un upgrade di dipendenza può alterare l'hash e forzare un bump di `SCHEMA_VERSION` senza alcun cambio di contratto. Oggi gli schemi sono solo enum/stringa, quindi il rischio è futuro; da rivalutare quando si aggiunge il primo schema non banale a un contratto o sezione.

## Deferred from: code review of 2-4-bootstrap-della-library-penpot-sui-contratti (2026-09-12)

- **`fontFamilies` non legato ai token (AC #1)**: `applyToken` rifiuta i token `fontFamilies` su Penpot 2.17.2, quindi il font resta fuori da design, reader e verifica. È la stessa voce `applyToken`/`fontFamilies` già registrata dalla story; va ripresa quando cambia l'API di Penpot.
- **`--font-sans` / `--font-serif` senza stack di fallback** in `packages/tokens/src/tailwind-theme.css`: se Manrope non viene caricato, i consumer di `font-sans` finiscono sul serif di default del browser. Lo genera la pipeline token, che per AC #4 della 2.4 non si tocca. Da risolvere nel generatore, oppure con un token di fallback, in una story dedicata.
- **`pds-setup` → `cleanup-legacy.py --also-remove _config`**: in un progetto gestito da `npx bmad-method install` cancellerebbe `_bmad/_config/`, `_bmad/core/` e `_bmad/pds/`. È il boilerplate del builder, identico in `bmad-bmb-setup`. Inoltre `_bmad/pds/config.yaml` è committato e contiene `user_name`. Da valutare a livello di framework prima di rilanciare una skill di setup.
- **Stroke senza stroke esistente** (non verificato, medium se confermato): `applyToken` su `strokeColor`/`strokeWidth` di divider, chevron e root dell'Input, shape nate senza stroke. Per chiarirlo: controllare in "Page Builder DS" che lo stroke sia davvero visibile.
- **Il gate `scripts` non intercetta import relativi fuori dal package** (`../../apps/web/src/x`, `../../ui/src`): preesistente, il gate prima della 2.4 non li copriva. Da riprendere insieme all'action item della retro Epic 1 sui gate.

## Deferred from: code review of 2-4-bootstrap-della-library-penpot-sui-contratti (2026-09-12, secondo pass)

- **fontFamilies non legato ai token (AC #1)** — limite dell'API Penpot 2.17.2: `applyToken` rifiuta i token `fontFamilies` ("should be a set of strings"). Il font resta una scelta del designer in Penpot; da riaprire quando l'API lo supporta.
- **`--font-sans: Manrope` senza stack di fallback** — il fallback lo produce la pipeline token (`theme-generator.ts`), che per AC #4 non si tocca in questa story; da valutare in 2.5+.
- **`pds-setup` lancia `cleanup-legacy.py --also-remove _config`** — in un progetto gestito dall'installer cancellerebbe `_bmad/_config/`, `_bmad/core/` e `_bmad/pds/`; inoltre `_bmad/pds/config.yaml` contiene `user_name`. È boilerplate del builder (identico a `bmad-bmb-setup`): da valutare a livello di framework BMad.
- **`applyToken` su `strokeColor`/`strokeWidth` di shape senza stroke preesistente** (divider, chevron, root dell'Input) — non verificato (medium se confermato): controllare in "Page Builder DS" che divider e bordo dell'Input abbiano uno stroke visibile.
- **Il gate `scripts` non intercetta import relativi fuori dal package** (`../../apps/web/src/x`, `../../ui/src`) — preesistente: il gate a `HEAD` non li copriva neanche prima.

## Deferred from: build review of 2-5-estrazione-adeguata-ricette-per-parti-e-token (2026-09-12, loop 1)

- source_spec: `_bmad-output/implementation-artifacts/2-5-estrazione-adeguata-ricette-per-parti-e-token.md`
  summary: Il CLI `extract:component` non segnala una parte del contratto con zero binding token in ogni cella né chiavi d'asse extra in `variantProps` — coperte solo dal gate separato `verify:library`.
  evidence: BH#8+ECH#7+VG-other-2 — `collectPartBindings` valida solo layer con tokens (`extract-component.ts`), `cellKeyOf` itera solo gli assi del contratto; nella library verificata (regole 6/7 di `verifyLibrary`) il caso non è raggiungibile e il workflow documenta `verify:library` come prerequisito, quindi duplicare la verifica nel CLI è ridondanza. Da rivalutare se l'estrazione viene mai eseguita senza verify a monte.
- source_spec: `_bmad-output/implementation-artifacts/2-5-estrazione-adeguata-ricette-per-parti-e-token.md`
  summary: I fixture JSON embeddano internals Penpot volatili in `style` (id shadow, `style: "drop-shadow"`, `hidden: false`) — la re-estrazione churnerà su campi che la pipeline non consuma.
  evidence: BH#13 — visibile in `packages/scripts/src/recipes/input.fixture.json` (shadow con `id` UUID). Non rompe nulla oggi (`validate` non legge `style`), ma mina la riproducibilità diff-zero degli artefatti: la normalizzazione di `style` è una scelta di design da fissare nella Story 2.6 (emitter), che definisce cosa consume.

## Register: story 2.6 (2026-09-12)

- **"Alias shadcn puntati al barrel" (review 1-1, 2026-09-05) — CHIUSO in Story 2.6 (Task 0)**: `packages/ui/components.json` ha gli alias `components`/`ui` riallineati da `@penpot-ds/ui/editor` (il barrel) a `@penpot-ds/ui/domains` (directory), più l'export `"./domains/*"` nel package.json. Nota di scope: `apps/web/components.json` non è toccato in questa story (l'item originale lo nominava; l'emitter usa solo il `components.json` di `packages/ui`) — verificare il file di `apps/web` quando il CLI shadcn verrà usato lì.
- **BH#13 di 2-5 (normalizzazione `style` nelle fixture) — SUPERATO per il rendering, resta aperto per il solo gate drift**: l'emitter (Story 2.6) NON consuma `style` — legge solo `shape.tokens` via ricetta, quindi la normalizzazione non è più necessaria per il rendering e la riproducibilità diff-zero del **rendering** è garantita senza toccare le fixture. La re-estrazione (`extract:component`) churnerebbe comunque sui campi `style` volatili, e quella riproducibilità è ciò che il gate drift confronta (fixture committata vs live): la voce resta aperta SOLO per quel perimetro.
- **Decisione frozen sul gate drift (Story 2.6, opzione a)**: il gate drift è bloccante in CI SE E SOLO SE il runner raggiunge il server MCP Penpot; altrimenti skip documentato col motivo nel log (mai un verde finto) e riesecuzione manuale/nightly. Lo script esiste comunque (`gates:render`), con seam offline (`fetchLive`/`callTool`) e prova rosso/verde propria. Verificato verde contro Penpot live reale durante lo sviluppo della story.

## Register: test end-to-end della pipeline su Alert (2026-09-12, branch usa e getta `test/alert-pipeline`)

Test manuale dopo il merge della 2.6: un componente nuovo (Alert, dominio `feedback`, 3 parti, asse `option`) portato da contratto → Penpot → fixture/ricetta → `.tsx` e fatto evolvere nei quattro casi reali — componente nuovo, variante aggiunta dal contratto (`warning`), token cambiati dal designer in Penpot, variante aggiunta dal designer in Penpot (`info`). La pipeline ha tenuto: ogni divergenza è stata segnalata con messaggi nominativi (`verify:library`, gate drift, estrazione che fallisce senza scrivere) e ogni propagazione è arrivata al codice con diff mirati e 5/5 gate verdi. Il branch non è stato committato; il container `Alert` (4 celle, `alert@1`) resta nella library Penpot "Page Builder DS" finché non viene rimosso a mano. Pianificato dal correct-course del 2026-09-12 (`sprint-change-proposal-2026-09-12-v2.md`): problemi 1–6 e 8 e i comandi mancanti nella **Story 2.7**, la skill nella **Story 2.9**, il problema 7 nella **Story 2.11** — tutto prima della libreria (**Story 2.10**), che moltiplica i componenti.

**Problemi trovati:**

- **1. L'emitter ignora `judgment.a11y` (verde finto del gate a11y)** — `render-component.ts` legge solo `judgment.domain`: `a11y.role`/`ariaAttributes` non sono mai emessi e il binding non ha un campo per attributi statici. Alert ha `role: "alert"` nel giudizio e nella base shadcn, ma il `.tsx` generato non lo porta; axe non lo segnala (un `div` senza ruolo non è una violazione), quindi il gate a11y resta verde. Fix: emettere `role`/`aria-*` dal giudizio (o dal binding) + gate che verifichi la presenza nel `.tsx` generato di ciò che il giudizio dichiara.
- **2. Campi content che collidono con attributi HTML nativi** — il field `title` di Alert ombreggia l'attributo `title` del `div` (`AlertProps = ComponentProps<"div"> & { title?: string }`): TypeScript non protesta, il tooltip nativo diventa inutilizzabile. Nessun check lo rileva. Fix: regola in `defineContract` (o lint dei contratti) che rifiuti i nomi di field che coincidono con attributi HTML globali, oppure l'emitter che fa `Omit<>` e segnala.
- **3. Liste hard-coded nei test di `scripts` e `contracts`** — `library-plan.test.ts` / `penpot-writer.test.ts` mantengono una propria mappa dei design a 3 voci: con un quarto contratto nel registry falliscono **42 test** con `Design mancante per il contratto "alert"`; `contracts/tests/components.test.ts` asserisce "esattamente i tre contratti". Stessa radice di BH#4 della 2.6 (lista gate hard-coded), rimasta nel lato Penpot; anche `DESIGNS` in `library-cli.ts` è una mappa a mano. Fix: derivare design e liste da `designs/*.design.json` + `COMPONENT_CONTRACTS`, con un test di copertura design↔registry.
- **4. Manca un'operazione per aggiungere celle a un container esistente** — `add:library` crea solo container nuovi e segnala le differenze senza correggerle: una variante aggiunta dal contratto non ha una via CLI per arrivare in Penpot. Nel test è stata creata con uno script usa e getta (`cellStep` del writer + aggancio al `VariantContainer` via `execute_code` scritto a mano: `appendChild` + `setVariantProperty`) — fuori dalla regola "nessuna scrittura su Penpot fuori dai comandi CLI". Fix: operazione `addCell` nel piano di `add:library` (additiva, con la stessa guardia anti-duplicato).
- **5. Manca un modo per alzare la versione di un contratto già in Penpot** — il plugin data `pagebuilder/contract` non è modificabile dall'interfaccia Penpot e nessun comando lo aggiorna; con `alert@2` nel contratto e `alert@1` in Penpot l'estrazione fallisce (regola 3). Nel test si è ripiegato su `alert@1`. Fix: operazione di bump del plugin data (esplicita, confermata) oppure regola documentata "i contratti si versionano solo prima del primo container".
- **6. Le coppie di contrasto dei design sono elencate a mano** — `DESIGN_USAGE_PAIRS` in `library-spec.ts` non si aggiorna con i design: `color.warning` e `color.info` come testo su `color.card` (usati dall'Alert) non sono controllati da `verify:library` (regola 10), che resta verde senza misurarli. Misurati a parte con `contrastRatio`: warning 4.87:1, info 7.96:1 — oggi sopra soglia, ma un cambio di valore del token non verrebbe intercettato. Nota: il 4.44:1 annotato in `library-spec.ts` riguarda warning su `color.background`, non su `card`. Fix: derivare le coppie testo/sfondo dai design (parte testo × parte che la contiene, per cella) invece che da una lista.

- **7. Le story generate non sono CSF3 valido** — `renderStoriesFile` emette le props al primo livello dell'oggetto story (`export const VariantInfo = { title: "title", description: "description", variant: "info" }`) invece che in `args`: Storybook ignora quelle chiavi e ogni story renderizzerebbe il componente senza props (tutte identiche). In più `title` è una chiave di metadati di Storybook e collide col field `title` di Alert (stessa famiglia del problema 2). Non rilevato da alcun gate perché Storybook non è installato (Story 2.11 in backlog; la 2.6 le ha emesse "CSF-shaped" in attesa). Fix: emettere `{ args: { … } }` e aggiungere, in 2.11, un gate che esegua le story (es. smoke test/`composeStories`). Da riprendere **in Story 2.11** (Storybook, ex 2.8 poi 2.10), o prima se la 2.10 rigenera molte story.

- **8. `verify:library` non segnala i container orfani** — la verifica itera i contratti del registry, non i container: un VariantContainer con plugin data `pagebuilder/contract` che dichiara un contratto inesistente in `@app/contracts` passa in silenzio. Osservato a fine test: con `alert` rimosso dal registry e il container `Alert` (`alert@1`, 4 celle) ancora in Penpot, `verify:library` era verde (0 errori). Il container è stato poi rimosso via MCP il 2026-09-12. Fix: regola in `verifyLibrary` — ogni plugin data `pagebuilder/contract` nella library deve corrispondere a un contratto del registry (errore nominativo sul container), con prova rosso/verde.

**Comandi mancanti (lavoro meccanico oggi fatto a mano):**

- **`adopt:variant -- <Comp>`** — quando il designer aggiunge una variante in Penpot, il flusso è: `verify:library` rosso con "valori in più" → decisione umana ("il valore entra nell'editor?") → quattro passi meccanici (valore nel contratto, bump `SCHEMA_VERSION` + voce in `contracts.fingerprint.json`, valore nel binding per assi `option`, cella nel design derivata da Penpot) → extract/render/gates. Il comando dovrebbe proporre il diff, chiedere conferma e applicarlo; fallire nominativamente quando la variante non è esprimibile (proprietà che varia con due assi, assi `state`/`behavior` senza mapping 1:1).
- **`addCell`** in `add:library` — vedi problema 4.

**Piano: skill `pds-component` (modulo `pds`)** — oggi `pds-bootstrap`/`pds-additive` coprono solo il lato Penpot (container per contratti esistenti); nessuna skill guida il resto del ciclo di vita di un componente (contratto, design, giudizio, binding, base shadcn, estrazione, render, barrel, gate). La skill leggerebbe lo stato (`verify:library` + `gates:render`) e sceglierebbe il percorso — nuovo componente (richiamando `pds-additive` per il container), token cambiati (drift → riestrazione), variante nuova da Penpot (`adopt:variant`), variante nuova dal contratto (`addCell`), valore literal in Penpot (regola 7 → al designer) — fermandosi solo sulle decisioni umane; pass/fail sempre negli exit code, mai nel prompt (AD-11). **Prerequisiti, in ordine:** (a) problema 3, altrimenti la skill porta ogni componente nuovo a test rossi; (b) `addCell` e `adopt:variant`, altrimenti la skill dovrebbe scrivere su Penpot o sui contratti con codice improvvisato; (c) problema 1, o almeno il check di presenza di `role`/`aria-*` nel `.tsx` generato. Da costruire con `bmad-workflow-builder`, sul modello di `pds-additive`.

**Decisione sulla suite `pds` (Alessandro, 2026-09-12): opzione A — si aggiunge, non si ristruttura.** Rilettura della suite: `pds-bootstrap` e `pds-additive` seguono già il modello giusto (guida + domande, scrittura solo via CLI, esito dall'exit code di `verify:library`) e il test Alert non ha trovato difetti nei loro prompt, ma nei comandi sottostanti — quindi nessuna riscrittura. Scartata l'opzione B (fondere bootstrap e additiva in `pds-library`): più ordinata ma sposta testo senza risolvere nulla di quanto emerso. Interventi:
- **nuova `pds-component`** con due voci di menu: **[PC] Crea componente** (componente assente in Penpot e nel codice: contratto, design, container via `pds-additive`, giudizio, binding, base, estrazione, render, barrel, gate) e **[PS] Sincronizza componente** (componente esistente: legge lo stato con `verify:library` + `gates:render` e sceglie il percorso — drift → riestrazione; valore in più da Penpot → `adopt:variant`; cella mancante → `addCell`; literal → al designer). L'estrazione non è una voce a sé: è un passo di entrambe. Sotto restano i comandi CLI separati; la skill li orchestra e si ferma sulle decisioni.
- **`pds-additive`, passo 3**: oggi "differenze: riportare e basta" è un vicolo cieco per cella mancante e valore d'asse in più; dopo averle riportate deve indicare dove si risolvono (`pds-component` → Sincronizza), senza correggere nulla da sé.
- **`pds-setup`**: registrare le nuove voci in `.claude/skills/pds-setup/assets/module-help.csv` (sorgente di `_bmad/pds/module-help.csv`); `pds-bootstrap` invariata.
- **Ordine**: prima i prerequisiti (a)–(c) sopra, poi la skill; la struttura di `pds-component` (domande, percorsi, guardrail) può essere progettata in anticipo con `bmad-workflow-builder` e implementata quando i comandi esistono.

## Deferred from: bmad-build split della Story 2.7 (2026-09-13)

- source_spec: none
  summary: Story 2.7 parte B "comandi di evoluzione" — `addCell` in `add:library` (additiva, guardia anti-duplicato), `adopt:variant -- <Comp>` (rileva, propone il diff, applica su conferma contratto + `SCHEMA_VERSION`/fingerprint + binding + design, fallisce nominativamente sui casi non esprimibili) e la regola di versionamento `nome@versione` di un contratto già in Penpot, documentata in `penpot-pipeline.md` con il comando o vincolo che la applica.
  evidence: split deciso da Alessandro al controllo di scope di `bmad-build` sulla 2.7: gli AC sono 7 deliverable indipendenti; la parte A (liste derivate, a11y dal giudizio, `verify:library` orfani+contrasto, rifiuto field con nomi HTML globali) va per prima perché `addCell`/`adopt:variant` scrivono design e contratti e dipendono dalle liste derivate. La 2.7 resta aperta in sprint-status finché anche la parte B non è done.

## Register: forge "pipeline troppo vincolante per il designer" (2026-09-13)

Sessione di Alessandro con agente forge (`_bmad-output/forge/pipeline-troppo-vincolante-per-il-designer/`), mentre la Story 2.7 parte B è in deferred. Esito pianificato da `sprint-change-proposal-2026-09-13.md`:

- Registro unico delle proprietà Penpot, nessuno skip (supportata o bloccata), blocco **per componente**, estensione dell'emitter una volta per tutte, attriti di file normalizzati → **Story 2.8** (nuova); normativo in `penpot-pipeline.md` (§ Registro delle proprietà e fedeltà).
- Percorsi [PS] della skill aggiornati ai nuovi stati → **Story 2.9**.
- Tag cliccabile = link (`href` dai dati commerce), X del filtro = composizione FilterChip, primitive rigide / composizioni elastiche → `design-system.md` e **Story 3.4** (caso di prova).
- **Aperti:** `applyToken` sulle ombre (Penpot 2.17) non verificato dal vivo; `compoundVariants` per una proprietà che varia con due assi (stesso principio "una volta per tutte"); il caso "variante esistente modificata" è rimandato, ma il fix dell'emitter "proprietà assente → classi per variante" serve comunque alle varianti nuove.

## Deferred from: build review of 2-7a-niente-verdi-finti (2026-09-13, loop 1)

- source_spec: `_bmad-output/implementation-artifacts/2-7a-niente-verdi-finti.md`
  summary: Le coppie di contrasto del focus ring non si ricavano dai design — `color.ring` su `color.card` (AccordionItem) resta dichiarata a mano in `CATALOG_PAIRS` (`library-spec.ts`), e un ring nuovo di un binding su un altro sfondo non verrebbe misurato.
  evidence: ECH#11 — il ring viene dalle classi strutturali `focus-visible:ring-ring` del binding shadcn, che `deriveDesignContrastPairs` non legge; limite preesistente (i binding non sono mai stati una sorgente di coppie). Decisione di Alessandro: esprimere il focus ring nel design (cella `focus` con `strokeColor`) oppure derivare le coppie anche dalle classi `ring-*` dei binding.

## Deferred from: bmad-build split della Story 2.7 parte B (2026-09-13)

<!-- Corregge la voce "split della Story 2.7" (2026-09-13) qui sopra: la parte B è `addCell` + regola A (bump plugin data), non `adopt:variant`, che è diventata la parte C. -->
- source_spec: `_bmad-output/implementation-artifacts/2-7b-comandi-di-evoluzione.md`
  summary: Comando `adopt:variant -- <Comp>` — rileva i valori d'asse presenti in Penpot e assenti dal contratto, stampa il diff e con `--yes` aggiorna contratto (AST TS), `SCHEMA_VERSION` + voce fingerprint (payload condiviso in `packages/contracts/src/`), binding `option` e cella di design ricavata da Penpot (`partBindings`); errore nominativo per assi `state`/`behavior`, proprietà che varia con due assi (funzione estratta da `computePartClasses`) e celle con literal. Per la regola A (decisa 2026-09-13) un'adozione è un cambio compatibile: alza solo `SCHEMA_VERSION`, non `contract.version`.
  evidence: split deciso da Alessandro al gate di dimensione della parte B (spec a ~2900 token): `addCell` + regola di versionamento vanno prima, perché l'adozione dal contratto verso Penpot passa da `addCell` e la regola decide se l'adozione tocca il plugin data.

## Deferred from: build review of 2-7b-comandi-di-evoluzione (2026-09-13, loop 2)

- source_spec: `_bmad-output/implementation-artifacts/2-7b-comandi-di-evoluzione.md`
  summary: Nessun comando rimuove le celle di valori d'asse tolti o rinominati (cambio incompatibile): dopo `bump:contract` le celle vecchie restano nel container e `verify:library` (regole 4/5) resta rosso finché il designer non le cancella a mano in Penpot.
  evidence: BH#5 del loop 2 — la modalità additiva per costruzione non cancella mai; la regola A è documentata in `penpot-pipeline.md` con il passo manuale. Da riprendere se i cambi incompatibili diventano frequenti (es. un `remove:cells` con `--dry-run`/`--yes` e guardia sul contratto corrente), probabilmente insieme alla skill `pds-component` (Story 2.9, voce [PS]).

## Deferred from: code review of story 2-7 gruppo 3 (2026-09-13)

- **"Report di PR/CI" da definire:** la promessa "i componenti in attesa devono essere visibili nel report di PR/CI, non solo nel terminale" è ripetuta in cinque artefatti (memlog, forged-idea, proposta, epics, penpot-pipeline) ma nessuno definisce dove vive il report, chi lo produce o come verificarlo. Da fissare nella spec di Story 2.8 (registro delle proprietà e blocco per componente), che è la story chiamata a implementarlo.

- source_spec: none
  summary: Story 2.8 parte B — valutazione per componente: verify:library e gates:render per voce, componenti in attesa nel report di PR/CI, normalizzazione di maiuscole/spazi/ordine assi, alias dei layer nel binding, cella mancante che blocca il componente con domanda al designer.
  evidence: split dalla Story 2.8 il 2026-09-13 (bmad-build, controllo multi-goal): consegna mergiabile da sola, si appoggia sul registro della parte A.
- source_spec: none
  summary: Story 2.8 parte C — estensione dell'emitter una volta per tutte: una variante senza una proprietà che il default ha (outline senza fill) produce classi per variante invece di stare nella base cva.
  evidence: split dalla Story 2.8 il 2026-09-13 (bmad-build, controllo multi-goal): consegna mergiabile da sola, si appoggia sul registro della parte A.
- source_spec: `_bmad-output/implementation-artifacts/2-8a-registro-delle-proprieta.md`
  summary: (high, non verificato) La regola icona vale solo per `strokeWidth` e solo nell'emitter. Se il chevron di AccordionItem (layer path) ha in Penpot `strokeAlignment` `center` (il default tipico dei path), alla prossima rilettura live `verify:library` e l'estrazione bloccano AccordionItem per geometria d'icona.
  evidence: `layerTreeProblems` non guarda il tipo di layer e `penpot-writer.ts` non imposta `strokeAlignment`. Si chiude con una rilettura live del chevron (con `PENPOT_MCP_TOKEN` caricato, oggi rifiutata) per vedere il suo `strokeAlignment`. Se è `center`, serve una decisione umana: estendere la regola icona anche a `strokeStyle`/`strokeAlignment` (riga del registro + lettura in `layerTreeProblems`).
- source_spec: `_bmad-output/implementation-artifacts/2-8a-registro-delle-proprieta.md`
  summary: (medium, non verificato) Fedeltà inversa: una classe strutturale della base (es. `border`, `disabled:opacity-50`) finisce nel codice anche se il design in Penpot non ha quella proprietà (nessuno stroke, nessuna opacità nella cella).
  evidence: comportamento delle classi strutturali precedente alla 2.8, emerso nella review (BH). Si verifica confrontando, per ogni classe strutturale del binding, le proprietà presenti nelle celle della ricetta (es. Badge: `border` in base; lo stroke c'è nel design?).

## Deferred from: verifica live Story 2.9 [PS] su Alert (2026-09-14)

- source_spec: `_bmad-output/implementation-artifacts/2-9-skill-pds-component.md`
  summary: la regola 10 di `verify:library` (contrasto) misura le coppie ricavate dal design committato (`designs/*.design.json`) e non i valori live di Penpot, e nessun comando aggiorna il design committato dopo un drift — un contrasto peggiorato in Penpot passa con tutti i gate verdi.
  evidence: nel giro 2 di [PS] su Alert il token `color.muted-foreground` è finito sulla root della cella `status=warning`: heading 1.93:1 e description 1.82:1 su `#3e4942`, mentre `verify:library` e `gates:render` erano verdi (verde finto). Dopo il riallineamento `alert.design.json` resta con `description` `warning` = `color.card-foreground` mentre Penpot e la fixture hanno `color.muted-foreground`. Rimandato da Alessandro (opzione a) come **prerequisito della Story 2.10**, che moltiplica i componenti.

## Deferred from: code review Story 2.9 (2026-09-14)

- source_spec: `_bmad-output/implementation-artifacts/2-9-skill-pds-component.md`
  summary: il giudizio ammette un solo `role` fisso per componente, quindi un componente con stati informativi e bloccanti (Alert: `info`/`success` vs `warning`/`error`) non può avere `role="status"` sugli uni e `role="alert"` sugli altri.
  evidence: Alert emette `role="alert"` (live region assertiva) anche per `info`/`success`, che interrompe lo screen reader a ogni render; limite di espressività pre-esistente del giudizio (`a11y.role` stringa unica), non introdotto dalla 2.9. Da decidere prima che la 2.10 generi Toast/Alert definitivi.
- source_spec: `_bmad-output/implementation-artifacts/2-9-skill-pds-component.md`
  summary: `_bmad/_config/skill-manifest.csv` registra le skill pds con path `_bmad/pds/<skill>/SKILL.md`, che su disco non esistono (le skill stanno in `skills/`, `.claude/skills/`, `.agents/skills/`).
  evidence: vale per pds-additive, pds-bootstrap, pds-setup e ora pds-component (schema dell'installer copiato); un consumer che risolve le skill dal manifest non le trova. Pre-esistente; da verificare con il flusso di installazione del modulo (pds-setup / installer BMad).

## Deferred from: bmad-build riordino di packages/scripts (2026-09-14)

- source_spec: `_bmad-output/implementation-artifacts/spec-riordino-packages-scripts.md`
  summary: Regole della library duplicate fra estrazione (`component-reader.ts`, `extract-component.ts`) e `verify-library.ts` (regole 2, 3, 6, 7: nome del container, versione del plugin data, parti con binding, valori senza token) portate in un modulo condiviso, così la futura elasticità dell'estrazione si applica in un posto solo.
  evidence: split dal riordino (controllo multi-goal, scelta di Alessandro "A+C ora, B dopo"): cambia la logica e non solo la posizione dei file, quindi rischia di alterare messaggi e ordine dei controlli dentro un refactor che deve lasciarli identici. Prerequisito del correct-course sull'elasticità dell'estrazione, prima della 2.10.
- source_spec: `_bmad-output/implementation-artifacts/spec-riordino-packages-scripts.md`
  summary: Aggiornare l'albero di `packages/scripts` in `ARCHITECTURE-SPINE.md:241-245` (oggi `penpot/ recipes/ render/ gates/`) alla struttura del riordino (`src/{shared,theme,extract,library,emitter,cli}` + `data/`), in una PR `chore/` separata.
  evidence: decisione 2a di Alessandro (2026-09-14): i planning-artifacts non si toccano dal branch del riordino; senza questa voce l'obbligo resterebbe solo nella sezione congelata della spec (review, blind-hunter).
- source_spec: `_bmad-output/implementation-artifacts/spec-riordino-packages-scripts.md`
  summary: Nessun test esegue `generate:theme` sui path di default (`PATHS.catalogPath` → `PATHS.tokensSrcDir`): lo smoke lo lancia solo con `--bogus`, che fallisce nel parsing prima di toccare i file.
  evidence: review verification-gap: un collegamento sbagliato dei path (chiave diversa ma esistente) passerebbe `paths.test`, il drift test di `theme-generator.test.ts` (chiama `generateTheme` direttamente) e lo smoke. Chiuderlo richiede comportamento nuovo: una modalità `--check` che confronta senza scrivere, o un seam per la cartella di output, perché un run reale riscrive file committati in `packages/tokens/src` mentre altri test li leggono.
- source_spec: `_bmad-output/implementation-artifacts/spec-riordino-packages-scripts.md`
  summary: `generate:theme` non chiama `process.exit` a fine run, a differenza degli altri entrypoint di `src/cli/`: con `--live` un transport MCP aperto può tenere vivo il processo.
  evidence: pre-esistente (nel baseline `src/generate-theme.ts` usava già solo `process.exitCode = 1`); gli altri CLI chiudono esplicitamente per i socket MCP/SSE (commento in `cli/extract-component.ts`). Da verificare con un run `--live` reale: se il processo resta appeso, allineare la chiusura agli altri entrypoint.
