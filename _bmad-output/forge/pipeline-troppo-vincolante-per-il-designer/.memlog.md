---
idea: Rivedere la pipeline Penpot→codice: troppo vincolante per il designer
goal: Chiarire, testare e migliorare: quali vincoli servono, se la tesi regge, cosa allentare e come
status: complete
updated: 2026-09-13T17:00
---

- (note) Inventario vincoli lato designer (da penpot-pipeline.md + verify-library/adopt-variant/render-component): token obbligatorio su ogni stile; nomi layer = parti del contratto; container PascalCase + plugin data scritto solo da skill; prodotto cartesiano completo senza celle in più; ordine assi = contratto; valori kebab minuscoli; proprietà che varia con un solo asse (no compoundVariants); niente rimozione di proprietà in una variante; parti a profondità 1; variante nuova in Penpot entra solo con adopt:variant deciso da dev; nessun comando per rimuovere celle.
- (decision) Il designer di riferimento è B: designer non tecnico che lavora in Penpot da solo, senza Claude/skill, e consegna il file dopo.
- (lock) Stili legati ai token: vincolo giusto e accettato anche per il designer non tecnico.
- (crack) Il vero dolore non sono i singoli vincoli ma il fallimento tutto-o-niente: il progetto arriva finito o quasi, la pipeline si blocca e il designer deve ripassare su tutti i componenti per adattarli alle regole.
- (lock) Penpot resta la fonte del design. Qualsiasi adeguamento per far passare la pipeline (ricetta, binding, emitter) è responsabilità dello sviluppatore; al designer non si chiede di ritoccare Penpot per esigenze della pipeline, solo per scelte di design vere (es. un colore senza token).
- (note) Caso Badge outline (fill rimosso + border aggiunto) = limite dell'emitter (classi del default nella base cva), non errore del designer: si risolve nel codice.
- (note) Raggio del blocco oggi: extract e render sono per componente, ma verify:library e gates:render (bloccante in CI) valutano tutti i componenti insieme; un solo componente fuori regola rende rossa la CI per tutti.
- (direction) Alessandro vuole che all'estrazione una variante aggiunta o modificata in Penpot passi e il codice generato rispetti il design, invece di bloccare.
- (crack) Variante AGGIUNTA in Penpot = nuovo valore di prop che le pagine salvano: adottarla in automatico riapre il lock 'il contratto è del page builder' (AD-6, schemaVersion). Variante MODIFICATA con valore esistente = già prevista senza agent (spec r.89): i blocchi lì sono limiti dell'emitter.
- (decision) Caso 1 (variante esistente modificata) rimandato per ora.
- (crack) Rimandare il caso 1 non basta: una variante nuova outline senza fill passa adopt:variant e poi si blocca nell'emitter ('rimozione di una classe', render-component.ts:385-392). Il blocco è lo stesso limite dell'emitter, non il contratto.
- (assumption) Flusso dev per una variante nuova: 1 decisione umana (esporla agli editor), passi sul contratto meccanici via adopt:variant; la skill pds-component (2.8) guida e classifica gli errori, pass/fail resta negli script.
- (note) 'Estendere l'emitter' = cambiare una volta sola la regola di computePartClasses (render-component.ts:376-383): una proprietà assente in una cella non va nella base di cva ma nelle singole varianti. Lavoro sulla pipeline, non per componente; dopo, ogni variante senza una proprietà passa in automatico su tutti i componenti.
- (lock) Regola generale: quando la pipeline non sa esprimere un design valido fatto con i token, si estende l'emitter una volta per tutte; né il designer né lo sviluppatore del componente devono aggirarlo. Condizione di Alessandro: adeguarsi deve restare semplice per lo sviluppatore.
- (crack) Tracciato: aggiungere una proprietà assente nel default (es. strokeColor solo in outline) passa già oggi (border-<token>). Ma strokeWidth (token borderWidth) e opacity non emettono classi: skip con solo un log. Uno stroke da 2px sparisce in silenzio = verde finto; a 1px funziona per caso grazie a 'border' della base shadcn.
- (decision) Quando l'emitter non sa riprodurre fedelmente una proprietà: si blocca solo quel componente, con segnalazione nominativa; gli altri proseguono. Mai generare in silenzio una versione infedele.
- (crack) Analisi proprietà: spessore (token border-width.default/thick) e opacità (solo opacity.disabled) letti e obbligati al token, ma l'emitter li salta con log → perdita silenziosa; fix una tantum con utility a variabile (border-(length:--border-width-*), opacity-(--opacity-*)). Ombra: token shadow.sm/md/lg/ring, emessa shadow-<token> (applyToken shadow in Penpot non verificato). Tratteggio: il reader non legge strokeStyle, un tratteggiato diventa pieno in silenzio e nessun gate lo vede. Idem allineamento dello stroke (interno/centrato/esterno).
- (assumption) Lo stile del tratteggio (solid/dashed/dotted) è una parola chiave, non un valore: non può essere un token; va deciso se ammetterlo come literal da lista chiusa senza violare il lock sui token.
- (lock) Regola token precisata: ogni VALORE (colore, misura, spessore, opacità, ombra) deve avere un token; gli stili a parola chiave da lista chiusa (es. tratteggio solid/dashed/dotted) sono ammessi senza token.
- (direction) Prerequisito prima di allentare: ogni proprietà esprimibile in Penpot che la pipeline non sa trattare deve bloccare quel componente con un messaggio, non sparire (spessore, opacità, tratteggio, allineamento stroke oggi spariscono). Poi si insegnano all'emitter una alla volta.
- (lock) Attriti di organizzazione del file, divisi in tre: maiuscole/spazi nei valori e ordine degli assi normalizzati dalla pipeline; layer con nome diverso dalla parte = alias nel binding a cura dello sviluppatore; cella mancante = blocco del solo componente e domanda al designer (scelta di design vera, mai inventata).
- (note) Tratteggio, piano: reader legge strokeStyle; la proprietà vale come parola chiave da lista chiusa (solid/dashed/dotted); valori fuori lista (es. mixed di Penpot) o più stroke sullo stesso layer bloccano il componente; emitter mappa a border-solid/dashed/dotted con la stessa logica per variante. Passo minimo intermedio: rilevare il non-solid e bloccare il componente invece di perderlo.
- (decision) Per ora le proprietà silenziose (spessore, opacità, tratteggio, allineamento stroke) bloccano il componente; il supporto completo arriva quando un componente reale lo richiede. Condizione: aggiungerlo dopo non deve richiedere un rework lungo.
- (direction) Anti-rework: oggi una proprietà è sparsa in 4 punti (styleOf del reader, PROPERTY_TOKEN_TYPE, TYPE_UTILITY_PREFIXES, controlli di verify-library). Il passo minimo introduce un registro unico delle proprietà Penpot (lettura, tipo token o lista di parole chiave, stato supportata/bloccata, mappatura emitter): sbloccarne una = cambiare una riga + mappatura + test rosso/verde.
- (lock) Registro unico delle proprietà Penpot, letto da reader, verify:library ed emitter. Solo due stati: supportata o bloccata. Nessuno skip: una proprietà o genera codice fedele o blocca il componente; una proprietà Penpot assente dal registro blocca anch'essa. Il refactor non deve cambiare l'output attuale (Badge/Input/Accordion identici byte per byte).
- (lock) Variante nuova in Penpot, opzione (c): blocca solo quel componente, che resta all'ultima versione buona; entra con la decisione dello sviluppatore (skill pds-component / adopt:variant). I componenti in attesa devono essere visibili nel report di PR/CI, non solo nel terminale. Scartata (b) 'generata ma nascosta': secondo vocabolario per risparmiare minuti.
- (crack) Domanda aperta dopo la chiusura: Badge con variante che aggiunge un'icona X per rimuoverlo (filtro). Non è una variante di stile: aggiunge una parte (contratto badge ha parts root,label) e un comportamento (click, button, aria-label) che Penpot non può disegnare e che una pagina statica non sa dove mandare. Probabile componente diverso (chip/filtro) o sezione con logica, non un valore d'asse.
- (direction) Caso d'uso: nelle pagine pubbliche, tag su un prodotto che al click applica un filtro di ricerca. Proposta: il tag è un LINK (href verso la ricerca con il filtro in URL), non un pulsante con logica JS; l'href arriva dai dati commerce della ProductCard (CAP-5), la logica di ricerca resta nella sorgente esterna (SPEC r.102). Per la pipeline: stati hover/focus = assi state già supportati; <a> vs <span> = binding/contratto, non Penpot. Nodo aperto: la X del filtro attivo sarebbe una parte opzionale, oggi non supportata (ogni parte deve esistere in ogni cella).
- (note) X sul filtro attivo: bisogno futuro, usato come prova di elasticità del page builder. Opzioni: A asse removable (celle x2, presenza decisa dall'editor e non dai dati); B parte opzionale nel contratto legata a un field (emitter rende {href && <a>}, serve regola per lo stile nelle celle senza X); C composizione: X come componente separato accanto al Badge in una sezione/composizione FilterChip (spec r.86: la composizione è una sezione, non una ricetta).
- (lock) Primitive rigide, composizioni elastiche. Tag cliccabile = link (href dai dati commerce, logica di ricerca esterna). X sul filtro attivo = composizione (Badge + icona-link in un blocco 'Filtro attivo'), niente parti opzionali nelle primitive. Caso di prova per le sezioni della Story 3.4, che dovranno dare al designer la stessa libertà decisa per i componenti.
- (kill) Asse removable sul Badge (presenza della X decisa dall'editor e non dai dati) e parti opzionali nelle primitive (costo su verify/estrazione/registro, stile da inventare nelle celle senza X).
