---
topic: Ambiente di sviluppo locale Penpot self-hosted con server MCP abilitato, a supporto della pipeline Penpot→codice (Epic 2, a partire da Story 2.1)
updated: 2026-09-11T18:25
---

- (note) Ricerca completata: Penpot MCP è il server ufficiale (github.com/penpot/penpot, dir mcp/), gratuito, incluso nel self-host via immagine penpotapp/mcp; espone execute_code, high_level_overview, penpot_api_info, export_shape, import_image su porta interna 4401 (HTTP streamable) + 4402 (WebSocket task bridge). Il docker-compose ufficiale (docker/images/docker-compose.yaml) non espone porte host per il container mcp — va aggiunto esplicitamente.
- (note) Stato reale rilevato sulla macchina di sviluppo: esiste già uno stack Penpot self-hosted in esecuzione (docker compose project 'penpot', servizi frontend/backend/exporter/postgres/valkey/mailcatch, porta 9001), attivo da 24h, verosimilmente usato durante la fase UX (Sally). Il container penpot-mcp NON è presente in questo stack — è la causa diretta del blocco rilevato in Story 2.1 (nessun MCP raggiungibile).
- (constraint) Tutto il tooling deve restare gratuito — vincolo esplicito dell'utente. Penpot self-host è open source (MPL-2.0) e il server MCP è incluso senza costo aggiuntivo; nessun servizio a pagamento va introdotto.
- (constraint) AD-11 (Architecture Spine): Penpot è single source of truth dei valori di design — la fixture committata da Story 2.1/2.2 non può contenere valori inventati; se il catalogo Penpot reale non è ancora popolato con token/componenti veri, la generazione resta bloccata finché non lo è (nessun placeholder spacciato per dato reale).
- (decision) Decisione utente: estendere lo stack Penpot esistente aggiungendo il servizio penpot-mcp, non crearne uno isolato nel repo page-builder.
- (note) Individuato il compose file dello stack esistente via 'docker inspect' sul container in esecuzione: /home/alessandro/Scrivania/projects/penpot/docker-compose.yaml, working_dir /home/alessandro/Scrivania/projects/penpot — è una directory SORELLA del repo page-builder, non tracciata in questo repo git. Servizi attuali: penpot-frontend (porta 9001, PENPOT_VERSION:-latest NON pinnata), penpot-backend, penpot-exporter, penpot-postgres, penpot-valkey, penpot-mailcatch (porta 1080). Nessun servizio penpot-mcp presente.
- (decision) Conseguenza architetturale: questo spec NON produce un docker-compose dentro il repo page-builder. L'artefatto operativo è l'istruzione/snippet per estendere il compose esterno esistente (fuori dal controllo versione di page-builder) + il contratto stabile (URL/env var) che packages/scripts può assumere.
- (question) Risposta utente su fixture: 'ho una library di Penpot, ma forse sarebbe meglio definirne una nuova' — indica che il catalogo token/componenti reale in Penpot non è ancora deciso/finalizzato. È una decisione di design (contenuto della library Penpot), fuori scope di questo spec tecnico (ambiente dev). Resta open question qui; non blocca l'ambiente MCP, blocca solo il momento in cui Story 2.1 può generare la fixture reale.
- (capability) CAP-1: Un membro del team avvia/ha già in esecuzione un'istanza Penpot self-hosted locale con il servizio MCP ufficiale abilitato e raggiungibile da processi Node/tsx sull'host (fuori Docker). Success: 'curl http://localhost:<porta_mcp>/mcp' risponde (handshake Streamable HTTP) senza dover esporre nulla oltre a localhost.
- (capability) CAP-2: packages/scripts (Story 2.1+) assume un contratto stabile e documentato per raggiungere l'MCP (URL, eventuale variabile d'ambiente per override) senza hardcodare assunzioni fragili. Success: il valore di default funziona out-of-the-box con l'istanza locale standard descritta in questo spec, ed è overridabile via env per chi gira su porta diversa.
- (constraint) Il container penpot-mcp non espone porte host nel compose ufficiale (raggiungibile solo dalla rete Docker interna) — va aggiunta esplicitamente una voce 'ports: 4401:4401' (e 4402:4402 se si vuole il task bridge WebSocket) al servizio nel compose esistente.
- (non-goal) Non-goal: questo spec non definisce il contenuto della library/token Penpot (quali colori, quali componenti) — è una decisione di design a parte. Non-goal: nessun docker-compose viene aggiunto al repo page-builder — l'istanza Penpot resta gestita nella sua directory separata esistente. Non-goal: nessun setup per esporre Penpot/MCP oltre a localhost (niente HTTPS/reverse proxy/produzione).
- (event) SPEC.md derivato (CAP-1, CAP-2) + companion penpot-mcp-setup.md. Self-validate Pass 1 (coerenza, regole 1-6,8): PASS — capability con intent+success, intent WHAT non HOW, constraint tutte load-bearing, 4 non-goal espliciti, success signal testabile (curl + script MCP), ID stabili, prosa lean.
- (event) Self-validate Pass 2 (preservation): PASS — ogni claim rilevante (server MCP ufficiale gratuito, porte 4401/4402 non esposte di default, stack esistente in ~/Scrivania/projects/penpot fuori dal repo, vincolo gratuito, AD-11 no valori inventati, decisione utente su library da rivedere) è atterrato in SPEC.md o nel companion. Nessun drop wrapper-only rilevato.
- (event) Riconciliazione: SPEC.md era stato modificato fuori da bmad-spec dopo l'ultimo derive — si registrano qui i fatti già presenti nel rendering, così il memlog torna la fonte canonica.
- (decision) (2026-09-06) Il compose Penpot è stato spostato dentro il repo in docker/penpot/docker-compose.yml con name: penpot pinnato (preserva i volumi penpot_penpot_assets / penpot_penpot_postgres_v15). Supera la decisione precedente 'nessun compose nel repo'.
- (decision) (2026-09-06) Open question sulla library chiusa: si riusa la library Penpot esistente, validata in UX; contrasto del token border corretto in Penpot; ruolo mono deferred.
- (decision) (commit #20) penpot-mcp avviato in single-user con command [node, index.js], sovrascrivendo il default --multi-user dell'immagine, perché all'epoca non si trovava come generare lo userToken.
- (direction) Richiesta utente 2026-09-11: gestire un token per l'MCP Penpot — configurare Penpot perché lo generi, collegare il server MCP su Claude Code e OpenCode con il token, far leggere il token agli script; il token non deve mai finire nel repo.
- (note) Verificato sull'istanza in esecuzione: Penpot 2.17.2. Il frontend contiene la sezione Settings→Integrations 'MCP server' (toggle, chiave, regenerate, snippet mcpServers con url {mcp_server_url}?userToken=<token>), visibile solo col flag enable-mcp. PENPOT_FLAGS attuale non lo contiene: è per questo che il token 'non veniva generato'.
- (note) Verificato: con enable-mcp in PENPOT_FLAGS l'entrypoint del frontend genera un proxy nginx su :9001 — /mcp/stream → penpot-mcp:4401/mcp, /mcp/sse → /sse, /mcp/ws → penpot-mcp:4402 (override con PENPOT_MCP_URI / PENPOT_MCP_URI_WS). Oggi il flag è assente, quindi .mcp.json che punta a :9001/mcp/stream non raggiunge l'MCP.
- (note) Verificato: il container penpot-mcp gira con 'Multi-user mode: false'. L'immagine espone solo --multi-user come opzione CLI; il resto si configura via env PENPOT_MCP_* (SERVER_HOST/PORT, WEBSOCKET_PORT, REMOTE_MODE, REDIS_URI, TOOL_TIMEOUT_S...).
- (note) Drift rilevato: il companion dichiara l'override PENPOT_MCP_URL, ma penpot-reader.ts e component-reader.ts hardcodano DEFAULT_PENPOT_MCP_URL=http://127.0.0.1:4401/mcp e non leggono alcuna variabile d'ambiente. CAP-2 non è davvero soddisfatta.
- (capability) CAP-3: Penpot self-hosted genera e rigenera, dalle impostazioni utente, un token MCP personale. Success: con lo stack avviato, Settings→Integrations mostra la sezione MCP server attiva con un token copiabile, e rigenerarlo invalida il precedente.
- (capability) CAP-4: Claude Code e OpenCode si collegano al server MCP Penpot usando il token personale, con configurazione condivisa nel repo che non contiene il token. Success: /mcp in Claude Code e l'elenco MCP di OpenCode mostrano penpot connesso con i tool (execute_code, high_level_overview...) e git grep del token nel repo non trova nulla.
- (capability) CAP-2 (estesa): packages/scripts legge URL e token MCP da variabili d'ambiente (PENPOT_MCP_URL facoltativa con default, PENPOT_MCP_TOKEN facoltativo); senza token il percorso offline sulla fixture continua a funzionare; con endpoint che richiede token e token assente l'errore lo dice esplicitamente. Success: pnpm run dei reader con le sole env impostate legge il catalogo reale; senza env la pipeline offline passa.
- (constraint) Il token MCP non deve mai essere scritto in un file tracciato da git: vive solo in variabili d'ambiente o file ignorati (.env* è già in .gitignore). I file di configurazione condivisi (.mcp.json, opencode.json) lo referenziano solo come variabile.
- (constraint) Unica sorgente del token per tutti e tre i consumatori (Claude Code, OpenCode, script): la variabile d'ambiente PENPOT_MCP_TOKEN. Claude Code non carica .env da solo, quindi la variabile deve esistere nell'ambiente di shell/IDE.
- (decision) Endpoint di riferimento per i client MCP: quello pubblicato da Penpot, http://localhost:9001/mcp/stream?userToken=<token> (lo stesso snippet mostrato nelle impostazioni Penpot). Richiede enable-mcp.
- (assumption) Il token è necessario solo in modalità multi-user: si assume di dover togliere l'override command del commit #20 (tornare al default --multi-user dell'immagine) perché il token abbia effetto. Da verificare in implementazione.
- (assumption) Gli script adottano lo stesso endpoint dei client (default PENPOT_MCP_URL = http://localhost:9001/mcp/stream) e aggiungono userToken dalla env; l'esposizione diretta di 4401/4402 sull'host diventa superflua. Da confermare in implementazione.
- (question) In multi-user mode il plugin 'MCP Server' dentro Penpot va ancora connesso a mano (File→Plugins→Connect) in ogni sessione, o si lega automaticamente all'utente del token?
- (question) In multi-user mode l'endpoint diretto :4401/mcp senza userToken viene rifiutato o accetta connessioni anonime? Determina se le porte 4401/4402 esposte sull'host vanno chiuse per non lasciare un accesso senza token.
- (non-goal) Non-goal: gestione segreti centralizzata (vault, secret manager), rotazione automatica del token, uso del token in CI, esposizione dell'MCP oltre localhost. Il percorso offline/CI resta sulla fixture e non usa mai il token.
- (event) SPEC.md rigenerato (CAP-1..CAP-4, CAP-2 estesa) + nuovo companion mcp-token.md; penpot-mcp-setup.md allineato (URL e Auth rimandano a mcp-token.md). Pass 1 coerenza: PASS — 4 capability con intent+success, intent WHAT, constraint load-bearing (token fuori da git, sorgente unica PENPOT_MCP_TOKEN, flag enable-mcp), 4 non-goal, success signal dimostrabile, ID stabili.
- (event) Pass 2 preservation: PASS — richieste utente (token Penpot, Claude Code, OpenCode, script, token fuori dal repo) e fatti verificati (2.17.2, flag enable-mcp, proxy nginx /mcp/stream, multi-user false, drift PENPOT_MCP_URL non letto) atterrati in SPEC.md o mcp-token.md. Restano 2 assumption e 2 open question.
- (event) Implementazione (spec-penpot-mcp-token): enable-mcp in PENPOT_FLAGS, override command rimosso, log penpot-mcp "Multi-user mode: true". Reader di packages/scripts su PENPOT_MCP_URL/PENPOT_MCP_TOKEN via mcp-client.ts; .mcp.json e opencode.json alla root; .envrc in .gitignore.
- (note) Verificato 2026-09-11 in multi-user: initialize riesce senza token sia su :9001/mcp/stream sia su :4401/mcp; il rifiuto arriva come testo del tool ("No userToken found in session context" senza token, "No Penpot instance connected for user token" con token errato o plugin non connesso), non come HTTP 401. Il plugin MCP Server va ancora connesso. Assumption 1 e 2 verificate; open question 1 e 2 risposte. Resta aperta la decisione sulle porte 4401/4402 pubblicate su tutte le interfacce.
